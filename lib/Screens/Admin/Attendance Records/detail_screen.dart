import 'package:flutter/material.dart';

class DetalScreenOfUser extends StatelessWidget {
  final String userId;
  const DetalScreenOfUser({
    super.key,
    required this.userId,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
